[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "node apps/webhook/dist/index.js"
